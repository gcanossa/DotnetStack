services:

  db:
    image: mysql:8
    restart: unless-stopped
    ports:
      - "3306:3306"
    environment:
      MYSQL_ROOT_PASSWORD: development
    volumes:
      - db-data:/var/lib/mysql

volumes:
  db-data:
