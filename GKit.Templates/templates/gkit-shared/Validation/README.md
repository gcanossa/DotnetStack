# Validation

`AbstractValidatorBase<T>` subclasses. They are UI neutral: both adapters resolve them through
`AddGKitValidator<T, TValidator>()`, and section 9 of the UI proposal lists them among the types
that did not change in the split.
